#include <bits/stdc++.h>
int findDuplicate(vector<int> &arr) 
{
  map<int,int> ob;
  for(int i :arr) // for(int i=0 ;i<arr.size(); i++)
  {
      ob[i]++;
      if(ob[i]==2)
        return i;

  }
	
}
